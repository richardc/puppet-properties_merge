notice( properties_merge( template("properties_merge/t1/one" ) ) )

notice( properties_merge( template("properties_merge/t2/one" ), template("properties_merge/t2/two" ) ) )

notice( properties_merge( template("properties_merge/t3/one" ), template("properties_merge/t3/two" ) ) )
